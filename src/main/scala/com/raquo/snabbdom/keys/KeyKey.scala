package com.raquo.snabbdom.keys

///** Represents the key to set the special snabbdom property `key` */
class KeyKey {

  val name: String = "key"

//  override def := (nodeKey: String): KeySetter[N, D] = {
//    new KeySetter(this, nodeKey)
//  }
}
